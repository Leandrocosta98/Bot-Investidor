# Dependências (Pasta muito pesada)
node_modules/

# Variáveis locais (Nunca devem subir)
.env

# Banco de dados local (Para não sobrescrever o do Render)
*.db
*.db-journal
*.sqlite
database.db

# Logs e arquivos de sistema
npm-debug.log*
*.log
.DS_Store